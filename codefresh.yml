version: '1.0'

steps:

    build_image:
      title: Building Image
      type: build
      dockerfile: Dockerfile.gradle
      image_name: condo/condominium
      tag: master

    launch_composition:
      title: Launch Composition
      type: launch-composition
      composition: docker-compose.yml
      environment_name: 'condominium'
      entry_point: tomcat
